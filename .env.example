# ============================================
# Hospital Appointment Scheduler - Environment Variables
# ============================================
# Copy this file to .env and update with your values
# cp .env.example .env

# ============================================
# Backend Server Configuration
# ============================================
PORT=3001

# ============================================
# MySQL Database Configuration
# ============================================
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=hospital_appointments

# ============================================
# Frontend Configuration
# ============================================
# API Base URL (optional - defaults to http://localhost:3001/api)
# Uncomment and modify if your backend is on a different host/port
VITE_API_URL=http://localhost:3001/api

# ============================================
# Notes
# ============================================
# - DB_PORT: MySQL default port is 3306 (phpMyAdmin uses this port)
# - DB_PASSWORD: Leave empty if MySQL has no password, or set your MySQL root password
# - VITE_API_URL: Only needed if backend is not on localhost:3001
# - All VITE_* variables are exposed to the frontend
# - Other variables are only available on the backend
